ARG REGISTRYNAME
ARG PROJECT
ARG VERSIONBASE

FROM ${REGISTRYNAME}/${PROJECT}:${VERSIONBASE}

COPY ./web-app/app.py /opt/

ENTRYPOINT FLASK_APP=/opt/app.py flask run --host=0.0.0.0 --port=8000

EXPOSE 8000